% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_cache.R
\name{build_cache}
\alias{build_cache}
\title{Build a precomputation cache for grid evaluation}
\usage{
build_cache(
  design,
  prior,
  effect,
  accrual = NULL,
  n_trials = 10000L,
  cores = 1L,
  seed = NULL,
  threshold_grid = NULL,
  cross_core_reproducible = FALSE
)
}
\arguments{
\item{design}{A \code{adabay_design} object whose schedule represents the
union of all candidate look times.}

\item{prior}{A \code{adabay_prior} object.}

\item{effect}{Data-generating values, as in \code{\link[=evaluate_design]{evaluate_design()}}.}

\item{accrual}{Optional \code{\link[=set_accrual]{set_accrual()}} specification. \strong{Required} for
tte designs, which are always staggered: the recruitment rate fixes
the Poisson enrolment process whose arrival times drive the calendar-time
exposure cutoff. The tte endpoint currently supports only \code{model = "poisson"}.
When supplied (any endpoint) it is stored in the cache so that
\code{\link[=evaluate_design]{evaluate_design()}} on the cache reports the expected duration (exact,
from simulated calendar times, for tte; analytic for the other
endpoints, which have no simulated recruitment timeline).}

\item{n_trials}{Number of virtual trials.}

\item{cores}{Number of CPU cores. Defaults to 1.}

\item{seed}{Optional integer seed.}

\item{threshold_grid}{Named list with elements \code{efficacy} and
\code{futility}, each a numeric vector of effect-size thresholds to
cache. Defaults to \code{list(efficacy = design$delta_null, futility =
design$delta_null)}.}

\item{cross_core_reproducible}{Logical, default \code{FALSE}. When \code{TRUE},
the simulator is forced to run in a single driver process (the user-
supplied \code{cores} is ignored for the simulation pass) so that the
per-trial RNG draws stored in the cache are bit-identical across
worker counts. Pair with \code{cross_core_reproducible = TRUE} on any
downstream \code{\link[=evaluate_design]{evaluate_design()}} / \code{\link[=calibrate_design]{calibrate_design()}} calls if full
regulatory-grade reproducibility of the cached path is required.
See \code{\link[=evaluate_design]{evaluate_design()}} for the cost discussion.}
}
\value{
An object of class \code{"adabay_cache"}.
}
\description{
Implements the precomputation strategy of Section 6.1 of the manuscript.
Simulates \code{n_trials} virtual trials once at the union of all
candidate look times, computes per-look posterior tail probabilities at
all candidate effect-size thresholds, and stores the result. Subsequent
calls to \code{\link[=evaluate_design]{evaluate_design()}} or \code{\link[=calibrate_design]{calibrate_design()}} then evaluate any
candidate \eqn{(K, \boldsymbol{n}, p_{k,u}, q_{k,v})} configuration
without further simulation cost.
}
\examples{
\donttest{
  des <- set_design(endpoint     = "binary",
                    n_per_look   = c(760, 1520, 2280),
                    effect_scale = "risk_difference",
                    alternative  = "less")
  pri <- set_prior(endpoint = "binary",
                   arms = list(c = list(family = "beta", a = 1, b = 1),
                               t = list(family = "beta", a = 1, b = 1)))
  cache <- build_cache(des, pri,
                       effect         = list(theta_c = 0.33, theta_t = 0.28),
                       n_trials       = 200L,
                       seed           = 1L,
                       threshold_grid = list(efficacy = 0, futility = 0))
  print(cache)
}
}
