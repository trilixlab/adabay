% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_design.R
\name{evaluate_design}
\alias{evaluate_design}
\alias{evaluate_design.default}
\alias{evaluate_design.adabay_design}
\alias{evaluate_design.adabay_cache}
\title{Evaluate operating characteristics for a Bayesian group sequential design}
\usage{
evaluate_design(x, ...)

\method{evaluate_design}{default}(x, ...)

\method{evaluate_design}{adabay_design}(
  x,
  prior,
  decision,
  effect,
  accrual = NULL,
  n_trials = 10000L,
  cores = 1L,
  seed = NULL,
  cross_core_reproducible = FALSE,
  ...
)

\method{evaluate_design}{adabay_cache}(x, decision, look_subset = NULL, ...)
}
\arguments{
\item{x}{A \code{adabay_design} object (fused path) or a \code{adabay_cache} object
(cached path).}

\item{...}{Further arguments passed to methods (currently unused).}

\item{prior}{For the fused path only: a \code{adabay_prior} from \code{\link[=set_prior]{set_prior()}}.}

\item{decision}{A \code{adabay_decision} from \code{\link[=set_decision]{set_decision()}}.}

\item{effect}{For the fused path only: named list of data-generating
values; see the worked examples in the package vignettes for endpoint-
specific names.}

\item{accrual}{For the fused path only: a \code{\link[=set_accrual]{set_accrual()}} specification.
\strong{Required} for tte designs, which are always staggered: the
recruitment rate fixes the Poisson enrolment process whose arrival times
drive the calendar-time exposure cutoff. The tte endpoint currently
supports only \code{model = "poisson"}. Optional for the other endpoints (continuous,
binary, count), which have no simulated recruitment timeline, where it
is used only to report an analytic expected duration.}

\item{n_trials}{For the fused path only: number of virtual trials.}

\item{cores}{For the fused path only: number of CPU cores. Defaults to 1.}

\item{seed}{For the fused path only: optional integer seed.}

\item{cross_core_reproducible}{For the fused path only: logical, default
\code{FALSE}. When \code{TRUE}, the simulator is forced to run in a single driver
process (the user-supplied \code{cores} is ignored for the simulation pass)
so that the trial-level RNG draws are bit-identical across worker
counts. The posterior and aggregation passes are core-independent in
both modes. Set to \code{TRUE} for regulatory submissions or other settings
where reproducibility across hardware configurations is required; leave
\code{FALSE} for design exploration where wall-clock matters more than
cross-\code{cores} bit-identity. The cost is the loss of simulator
parallelism: small for binary, count and tte (where the simulator
is a small fraction of total wall-clock); moderate for continuous.}

\item{look_subset}{For the cached path only: optional integer vector. If
supplied, the design is evaluated at the subset of look indices given
(in order). Defaults to all looks in the cached design.}
}
\value{
An object of class \code{"adabay_oc"}, a list with elements:
\describe{
\item{\code{alpha}}{Overall type I error rate scalar (regulatory
convention: under non-binding futility, computed as if futility
were absent).}
\item{\code{eff_prob}}{Overall efficacy probability scalar under binding
trial conduct.}
\item{\code{fut_prob}}{Overall futility probability scalar under binding
trial conduct.}
\item{\code{eff_prob_at_look}, \code{fut_prob_at_look}}{Per-look stopping
probabilities under binding conduct; they partition the sample
space.}
\item{\code{alpha_at_look_binding}, \code{alpha_at_look_nonbinding}}{Alpha
spent at each look under binding and non-binding semantics
respectively.}
\item{\code{expected_sample_size}}{Expected total subjects (continuous,
binary), exposure (count) or events (tte) under binding
conduct.}
\item{\code{expected_duration}}{Expected calendar duration; present only
when an \code{\link[=set_accrual]{set_accrual()}} specification is supplied (fused path) or
was supplied to \code{\link[=build_cache]{build_cache()}} (cached path), and absent otherwise.
For tte this is the exact Monte Carlo mean of the calendar
time realised (during simulation) at each trial's own stopping
look; for continuous, binary and count -- which have no simulated
recruitment timeline -- it is an analytic approximation.}
\item{\code{tau}, \code{C}}{Per-trial stopping look and trial outcome code
(\code{+1}/\code{-1}/\code{0} for efficacy/futility/no-cross).}
\item{\code{n_trials}, \code{effect}, \code{design}, \code{decision}}{Echoes of the inputs
for downstream reproducibility. The fused path additionally records
\code{seed}, \code{call} and (when supplied) \code{accrual}; these are omitted on
the cached path.}
}
}
\description{
\code{evaluate_design()} is the single entry point for obtaining the
operating characteristics of a Bayesian group sequential design. It is
polymorphic in its first argument:
}
\details{
\describe{
\item{\code{adabay_design}}{Fused path: simulates \code{n_trials} virtual trials at
the supplied data-generating values, computes per-look posterior tail
probabilities of \eqn{\Delta} in closed form, and applies the supplied
decision rule to obtain the per-look stopping probabilities, the
overall type I error rate (or power under the alternative), and the
expected sample size, all in one call. Use this for one-off evaluation
of a single design.}
\item{\code{adabay_cache}}{Cached path: looks up pre-computed per-look tail
probabilities in a \code{adabay_cache} produced once by \code{\link[=build_cache]{build_cache()}} and
aggregates them under the supplied decision rule, without re-simulating
trials. Use this for grid-style calibration when many decision rules
share the same simulated trials.}
}

Slow inner integrals are dispatched to C++ implementations via
\pkg{Rcpp}; the trial-level loop is embarrassingly parallel and is
parallelised across \code{cores} workers via \code{\link[parallel:mclapply]{parallel::mclapply()}} (POSIX) or
\code{\link[parallel:parLapply]{parallel::parLapply()}} (Windows).
}
\section{Binding vs non-binding futility}{

The simulator always conducts virtual trials under binding semantics
(a trial physically stops at the first crossing of either the efficacy
or futility threshold). The per-look stopping probabilities
\code{eff_prob_at_look[k]} and \code{fut_prob_at_look[k]} therefore always reflect
actual binding conduct and partition the sample space:
\code{sum(eff_prob_at_look) + sum(fut_prob_at_look) = 1} for both binding
and non-binding settings of \code{futility_binding}.

The flag changes only the overall scalar type I error rate \code{alpha}:
\describe{
\item{Binding}{\code{alpha = sum(eff_prob_at_look)}: probability that the
trial crosses efficacy before any futility crossing under the
data-generating distribution.}
\item{Non-binding}{\code{alpha = sum(alpha_at_look_nonbinding)}: probability
that the trial would have crossed efficacy at some look had the
futility rule been ignored (the standard regulatory convention).}
}
}

\examples{
des <- set_design(endpoint = "binary",
                  n_per_look = c(760, 1520, 2280, 3040, 3800),
                  effect_scale = "risk_difference",
                  alternative = "less")
pri <- set_prior(endpoint = "binary",
                 arms = list(c = list(family = "beta", a = 1, b = 1),
                             t = list(family = "beta", a = 1, b = 1)))
dec <- set_decision(
  efficacy = list(list(threshold_effect = 0, threshold_prob = 0.99)),
  futility = list(list(threshold_effect = 0, threshold_prob = 0.90)),
  futility_binding = FALSE)
\donttest{
## Fused path: evaluate a single design directly
oc <- evaluate_design(des, prior = pri, decision = dec,
                      effect = list(theta_c = 0.33, theta_t = 0.28),
                      n_trials = 200L, cores = 1L, seed = 1L)
print(oc)

## Cached path: build once, evaluate many decision rules cheaply
cache <- build_cache(des, pri,
                     effect         = list(theta_c = 0.33, theta_t = 0.28),
                     n_trials       = 200L,
                     seed           = 1L,
                     threshold_grid = list(efficacy = 0, futility = 0))
oc2 <- evaluate_design(cache, dec)
print(oc2)
}
}
